# Screenshots

Generate launch screenshots with:

```bash
npm run build
npm run screenshots
```

The screenshot script starts a local Vite preview server, seeds fictional demo resume data, and writes PNG files to `public/screenshots/`.

Screenshots must use fictional or sanitized demo data only. Do not commit screenshots with private resume content, personal contact information, real job application details, or sensitive data.
